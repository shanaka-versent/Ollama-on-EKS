# OS
.DS_Store

# Claude Code settings
.claude/
CLAUDE.md

# Terraform (duplicated here for root-level git init)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/tfplan
terraform/errored.tfstate
terraform/.terraform.lock.hcl

# Lambda zip artifacts (generated by Terraform archive_file)
terraform/modules/**/lambda/*.zip

# Python cache
__pycache__/
*.pyc

# Environment variables (secrets)
.env

# TLS certificates
certs/

# Kong gateway config — contains real API keys, managed via deck sync only
# deck/kong-config.yaml  ← committed (no secrets, auto-synced via GitHub Actions)
# deck/kong-consumers.yaml.sample ← committed (template only, placeholder keys)
deck/kong.yaml
deck/kong-consumers.yaml

# Generated HTML reports (keep local for learning, auto-generated from markdown)
README.html
Ollama-EKS-Report.html
Auth-Flow-Guide.html
Platform-Analysis-Report.html
docs/auth-flow-guide.html
